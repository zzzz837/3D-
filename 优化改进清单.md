# 3D拟物Layout编辑器 — 优化改进清单

> 从用户角度梳理，按严重程度排序。共 **48** 项。

---

## 一、功能Bug（影响使用）

| # | 问题 | 位置 |
|---|------|------|
| 1 | **Delete键无响应**：菜单显示`Del`快捷键，但JS层没有监听Delete键，独立浏览器下无法删除cell | keydown handler L718-725 |
| 2 | **Ctrl+Z/Y无JS实现**：撤销/重做依赖Python Bridge，浏览器独立模式下不可用 | keydown handler L718-725 |
| 3 | **W键线框快捷键失效**：菜单标注了W键，但keydown没处理 | keydown handler L718-725 |
| 4 | **退出预览时模拟压力不停止**：关闭预览后SIM_INTERVAL仍在后台每40ms更新压力 | togglePreview L1067-1071 |
| 5 | **双重HEAT_INTERVAL**：同时开启预览+模拟时创建两个定时器（500ms和80ms），重复渲染 | L1067 + L1095 |
| 6 | **placeCell触发双重_notify**：monkey-patch导致每次放置cell发送两次状态同步 | L1182-1186 |
| 7 | **Ctrl+Click清空多选而非切换**：Ctrl+Click已选中的cell会清空所有选择 | MULTI模式 L598-606 |
| 8 | **overlapUI完全是空函数**：CO()计算了重叠但从不展示，用户无法知道哪些cell重叠 | L422/L424/L1335→L450 |
| 9 | **cell面积始终用圆形公式**：`A`对象从未填充真实曲面面积，面板显示的mm²是近似值 | L128/L188 |

---

## 二、UI/UX改进

| # | 问题 | 建议 |
|---|------|------|
| 10 | **"拖出下一个"按钮名不副实** | 没有拖拽功能，只是创建cell。建议改为"+添加Cell" |
| 11 | **"退出预览"按钮红色** | 红色=危险操作，退出预览是安全操作，改回灰色 |
| 12 | **viewport提示文字太长** | 8个操作挤在一行无法阅读，改成分段或动态提示 |
| 13 | **topZoom永远显示"—"** | 相机距离指示器从未更新，要么实现要么删掉 |
| 14 | **Ctrl指示显示"—"而非"OFF"** | 默认状态不明确 |
| 15 | **"工具"菜单只有一个条目** | 碰撞检测toggle，可合并到"编辑"菜单 |
| 16 | **菜单项与工具栏完全重复** | 菜单没有提供任何独占功能 |
| 17 | **无"全选"/"取消全选"** | 多选模式下缺少批量操作 |
| 18 | **H键隐藏Cell是调试功能** | 没有文档说明，用户误触会以为cell丢失 |
| 19 | **未加载模型时"下一个ID"显示#0** | 误导用户以为可以创建cell |

---

## 三、性能问题

| # | 问题 | 位置 |
|---|------|------|
| 20 | **鼠标移动做了无用raycast** | pointermove每66ms做一次intersectObject但丢弃结果 L674-678 |
| 21 | **renderLoop无空闲暂停** | 全局mousemove事件持续标记_needsRender=true L1169 |
| 22 | **rebuildFieldCache调用太频繁** | 每次滑块变化/cell移动都重建全量缓存 L854-887 |
| 23 | **RB()全量重建所有Cell mesh** | 修改一个cell却重建全部，应改为增量更新 L424 |
| 24 | **SIM_INTERVAL=40ms频率过高** | 模拟压力每秒更新25次，多数场景不需要 L1094 |

---

## 四、缺失功能

| # | 问题 |
|---|------|
| 25 | **无CSV/Excel导出**：只能导出JSON，用户无法在Excel中分析cell布局数据 |
| 26 | **无cell旋转控制**：cell数据模型有rot字段，但UI面板没有任何旋转输入 |
| 27 | **无批量属性编辑**：选中多个cell时，改半径/压力/标签只影响当前焦点cell |
| 28 | **模型加载无进度条**：大模型加载时UI冻结，只显示状态文字，无百分比或预计时间 |
| 29 | **模型加载无法取消**：fetch开始后用户无法中止，只能等待 |
| 30 | **属性修改不触发snapShot**：改cell半径/位置/压力后无法Undo恢复 |
| 31 | **导入新模型无确认对话框**：切换模型时已放置的cell静默清空 |
| 32 | **无自动排列功能**：用户必须逐一手动放置cell，没有自动填充/均匀分布功能 |

---

## 五、不合理的默认值

| # | 问题 | 当前值 | 建议 |
|---|------|--------|------|
| 33 | 未加载模型时通道数=0 | MAX=0 | 面板显示"请先导入模型" |
| 34 | 总通道数默认200 | bsTotal=200 | 根据导入的模型自动计算 |
| 35 | 力场半径默认60mm | FIELD_R=60 | 根据模型尺寸自适应 |
| 36 | 单位比例默认1mm/单位 | unitMM=1 | 模型加载后自动检测+调整 |
| 37 | Cell半径默认10mm | radius=10 | 可考虑根据模型曲面面积自动建议 |
| 38 | Undo深度50而不是文档写100 | 50 | AGENTS.md文档说100 |

---

## 六、死代码/未使用

| # | 内容 |
|---|------|
| 39 | `warmHeatColor()` 完整实现但从未被调用 |
| 40 | `S` 变量被设置但从未读取 |
| 41 | `_fieldVisualBoost` 对当前jetColor路径无实际效果 |
| 42 | `tbSaveFile`/`tbOpenFile` 按钮ID在JS中引用但HTML中不存在 |

---

## 优先级建议

**P0（立即修复，阻塞使用）：**
- #1 Delete键无响应
- #4 退出预览不停止模拟
- #5 双重定时器

**P1（严重影响体验）：**
- #2 #3 快捷键缺失
- #9 cell面积不准确
- #10 按钮名误导
- #30 属性修改不触发undo

**P2（体验优化）：**
- #12 提示文字优化
- #20 #21 性能浪费
- #25 导出格式
- #31 切换模型确认
- #32 自动排列

**P3（锦上添花）：**
- #13 #14 #15 #16 #19 UI细节
- #39 #40 #41 #42 清理死代码
