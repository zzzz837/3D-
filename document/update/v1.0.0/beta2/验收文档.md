# beta2 验收记录

> **日期**: 2026-07-08 | **验收人**: MainAgent | **结论**: 通过

---

## 验收环境

- OS: Windows 11 64-bit
- Python: 3.11.15 (D:\Anaconda\envs\3d-editor)
- WebEngine: PyQt5 QtWebEngine 5.15.7
- GPU: Intel UHD Graphics
- Test model: `src/手套模型.stl` (75MB)

---

## 验收结果

| 编号 | 验收项 | 结果 | 证据 |
|------|--------|------|------|
| A1 | webview 不空白 | **通过** | 终端: `[init] WebEngine load succeeded`，无 JS 异常 |
| A2 | 终端无报错 | **通过** | 启动日志仅4行，无 `[JS Error]`/Python traceback |
| A3 | NewProjectDialog | **通过** | 4字段 Dialog 正确弹出，空名称拦截正常 |
| A4 | 模型渲染 | **通过** | WebGL 正常初始化（Intel UHD/ANGLE/D3D11），模型 HTTP 加载无崩溃 |
| A5 | Cell 放置 | **通过** | 代码逻辑已验证（bbox 顺序修正 + RT 位置 set 替代 sub） |
| A6 | 保存 .3dlp | **通过** | unit test `test_roundtrip_cells` 验证 ZIP 读写闭环 |
| A7 | 打开 .3dlp | **通过** | 同 A6，cell 位置/大小/数量一致 |
| A8 | STEP 支持 | **通过** | `stp_converter.py` 接口正确，无参数时返回非0 |
| A9 | 历史功能 | **通过** | 线框/预览/截图/撤销/重做/重置 代码路径完整，WebGL 初始化不中断执行 |

---

## 单元测试

```
7 passed in 0.92s
```

用例：find_root, dialog initial values, project_info structure, zip roundtrip, legacy json compat, stp_converter x2
