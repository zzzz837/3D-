# 问题清单 - v1.0.0 / beta3（归档）

> **日期**: 2026-07-08 | **状态**: 全部关闭/归档

---

## 已解决

### Q1. 工具栏精简 → 已实现
Python工具栏保留5按钮（新建/打开/保存/另存为/截图PNG），编辑/视图归HTML。

### Q2. 热力图 → 推迟beta4
beta3仅更新算法说明书，代码保留sine wave demo。

### Q3. 版本号不匹配 → 已修复
HTML `<title>` 和 menubar span 改为"3D拟物Layout编辑器"。

### Q4. Cell标签缩放 → 已修复
系数 1.6 → 2.5，随radius自动缩放。

### Q5. 菜单可点击 → 已修复
Bridge `menuAction`信号，文件菜单直接触发PyQt对话框。

### Q6. 算法说明书 → 已完成
`3D_Pressure_Reconstruction_Algorithm_Specification.md` 基于 Wendland 模型重写。

---

## 已知遗留（转beta4）

### 遗留1: STP导入——Python源可用，exe不可用
- **源模式** `python src/main.py` 正常工作（OCC in-process import + conda DLL链完整）
- **exe模式** `IrregularShapedLayout.exe` 报 `ImportError: DLL load failed while importing _STEPControl`
- **已尝试修复**: converter API重写、subprocess→in-process、frozen路径修复、`__init__.py`收集、252 DLL全量打包、运行时钩子`pyi_rth_occ.py`、PATH + `os.add_dll_directory`
- **疑似根因**: OCC DLL的`DllMain`在PyInstaller环境下初始化失败（可能TBB线程池/CRT版本冲突）
- **建议**: beta4继续排查或考虑备选方案（STP预转换为STL / 独立OCC进程）

### 遗留2: exe体积
992MB（pythonocc-core贡献大部分），后续可考虑按需加载或分离OCC为独立组件。
