# 3D拟物Layout编辑器 — 力场渲染技术方案（完整版）

## 一、项目定位

**目标**：在3D曲面模型上放置触觉传感器单元(Cell)，实时模拟压力分布力场，以热力图形式可视化。

**环境约束**：QWebEngine (Chromium 83) + Three.js r160 + WebGL 2.0

**性能要求**：支持2000+ Cell同时渲染，画面稳定无抖动，力场圆形平滑过渡。

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Three.js 场景树                          │
├─────────────────────────────────────────────────────────────────┤
│  RT (Group) — 根变换节点，用于模型居中                           │
│  ├── M (Mesh, MeshPhongMaterial)                                │
│  │   → renderOrder=0, vertexColors=false                        │
│  │   → 纯灰色底衬 0x555d65，不参与热力图                         │
│  ├── HM (Mesh, ShaderMaterial) ← ★壳层★                        │
│  │   → renderOrder=3, transparent=true, depthWrite=false         │
│  │   → GPU逐像素Wendland核 + heatColor                           │
│  │   → 沿法线偏移0.6mm，避免z-fighting                          │
│  └── CG (Group) — Cell网格容器                                  │
│      └── 每个Cell一个Group:                                     │
│          ├── Mesh (MeshPhongMaterial, renderOrder=5)            │
│          │   → 圆盘，depthTest=false，颜色=p压力映射             │
│          ├── Line → 轮廓线                                       │
│          ├── Sprite → ID标签                                     │
│          └── Mesh → 中心点                                       │
└─────────────────────────────────────────────────────────────────┘
```

```
渲染顺序 (renderOrder):
  0: 模型      (最先渲染，写深度缓冲)
  3: 壳层      (覆盖模型，测试深度，不写深度)
  5: Cell圆盘  (最上层，不测试深度)
```

---

## 三、为什么用GPU纹理方案（方案选型）

### 3.1 尝试过的方案

| 方案 | 方式 | 结果 |
|------|------|------|
| CPU+Wendland+vertexColors | 模型顶点算Wendland→vertexColors | ✅稳定，❌低面数色带锯齿 |
| GPU onBeforeCompile | 注入Phong shader | ❌Chromium 83不可靠 |
| GPU ShaderMaterial(ShaderLib) | 三.js内置shader+手动展开include | ❌光照传不过 |
| GPU ShaderMaterial uniform数组 | 自写shader，uniform传cell数据 | ✅可用，❌上限256 cell |
| **GPU纹理方案（最终）** | Float32纹理传cell数据，shader采样 | ✅当前方案 |

### 3.2 纹理方案 vs uniform方案

| | uniform数组 | 纹理 |
|---|---|---|
| cell上限 | 256（GPU寄存器硬限） | 2048+（显存决定） |
| 数据传递 | `uniform vec3 uCP[256]` | `sampler2D uCellTex` |
| shader读取 | `uCP[i]` | `texture2D(uCellTex, vec2(tx, 0.5))` |
| GPU存储 | 寄存器（小容量） | 显存（大容量） |
| 兼容性 | 全平台 | 需FloatType纹理(WebGL 2.0原⽀) |

**选择纹理方案原因**：绕开GPU寄存器限制，用显存存cell数据，容量提升8倍(to 2048)。

---

## 四、核心算法：Wendland加权平均

### 4.1 数学定义

```
给定N个cell，每个cell有:
  cᵢ = (xᵢ, yᵢ, zᵢ)     — 3D世界坐标
  Rᵢ                    — 力场影响半径
  Pᵢ ∈ [0, 1]           — 压力值

对于模型表面的任意一点 x:
  rᵢ(x) = ‖x - cᵢ‖ / Rᵢ    — 归一化距离

  φ(r) = { (1-r)⁴·(4r+1)   if 0 ≤ r ≤ 1     ← Wendland C²核
         { 0                 if r > 1

  S(x) = Σ Pᵢ·φ(rᵢ(x)) / Σ φ(rᵢ(x))    ← 加权平均压力

  Color(x) = heatColor(S(x))             ← 映射到热力图色带
```

### 4.2 Wendland核函数特性

| r | φ(r) | 物理意义 |
|---|------|---------|
| 0.000 | 1.000 | 中心，100%强度 |
| 0.200 | 0.737 | 20%半径，74%强度 |
| 0.500 | 0.188 | 50%半径，19%强度 |
| 0.800 | 0.007 | 80%半径，<1%强度 |
| 0.900 | 0.0003 | 几乎消失 |
| ≥1.000 | 0.000 | 场边界，完全归零 |

**选择Wendland的理由**：
- C²连续（二阶可导）：视觉绝对平滑
- 紧支撑（r≥1精确归零）：无需截断，无无限尾巴
- 工业验证：SPH流体力学、无网格法标准核函数

### 4.3 多cell叠加时的效果

```
两cell交汇处（中点）：
  cellA贡献: P_A × φ(r_A)
  cellB贡献: P_B × φ(r_B)
  S(中点) = (P_A·φ_A + P_B·φ_B) / (φ_A + φ_B)  ← 距离加权平均

若P_A = P_B：中点颜色 = 两cell相同颜色 → 无缝融合
若P_A ≠ P_B：中点颜色 = 按距离比例混合 → 平滑渐变
```

**覆盖度**（透明度）：
```
cf = min(Σφ × 1.8, 1.0)

两cell中点：Σφ ≈ 0.6 → cf ≈ 1.0 → 完全不透明，无灰色缝隙
远离所有cell：Σφ → 0 → cf → 0 → 完全透明，露出模型底色
```

---

## 五、GPU着色器完整实现

### 5.1 顶点着色器（Vertex Shader）

```glsl
varying vec3 vWP;   // 世界坐标 — 用于距离计算
varying vec3 vWN;   // 世界法线 — 用于光照

void main() {
    vec4 wp = modelMatrix * vec4(position, 1.0);
    vWP = wp.xyz;
    vWN = normalize(mat3(modelMatrix) * normal);
    gl_Position = projectionMatrix * viewMatrix * wp;
}
```

### 5.2 片段着色器（Fragment Shader）

```glsl
// === 输入 ===
varying vec3 vWP;           // 当前像素世界坐标
varying vec3 vWN;           // 当前像素世界法线
uniform int uCellCount;     // 活跃cell数量（≤2048）
uniform sampler2D uCellTex; // cell数据纹理(4096×1, RGBA, FloatType)
uniform float uCellTexW;    // 纹理宽度(=4096)
uniform float uOp;          // 最大透明度(=1.0)

// === Wendland C²核 ===
float wd(float r) {
    if (r >= 1.0) return 0.0;
    float t = 1.0 - r;
    return t * t * t * t * (4.0 * r + 1.0);
}

// === 热力图色带 ===
vec3 hc(float t) {
    t = clamp(t, 0.0, 1.0);
    // 5个关键色：深蓝(0) → 亮蓝(0.2) → 金黄(0.5) → 橙红(0.7) → 纯红(1.0)
    vec3 c0 = vec3(0.05, 0.25, 0.75);  // 深蓝
    vec3 c1 = vec3(0.08, 0.55, 0.95);  // 亮蓝
    vec3 c2 = vec3(0.90, 0.75, 0.10);  // 金黄
    vec3 c3 = vec3(1.0,  0.35, 0.03);  // 橙红
    vec3 c4 = vec3(1.0,  0.05, 0.01);  // 纯红
    if (t <= 0.20) return mix(c0, c1, t / 0.20);
    if (t <= 0.50) return mix(c1, c2, (t - 0.20) / 0.30);
    if (t <= 0.70) return mix(c2, c3, (t - 0.50) / 0.20);
    return mix(c3, c4, (t - 0.70) / 0.30);
}

void main() {
    // === Step 1: 逐cell累加 ===
    vec3 ha = vec3(0.0);  // Σ(hc(Pᵢ) × φᵢ)
    float ws = 0.0;       // Σ(φᵢ)

    for (int i = 0; i < 2048; i++) {
        if (i >= uCellCount) break;

        // 从纹理读取cell i的数据
        // cell数据布局: texel[i*2]=(x,y,z,r)，texel[i*2+1]=(p,0,0,0)
        float tx  = (float(i) * 2.0 + 0.5) / uCellTexW;
        float tx2 = (float(i) * 2.0 + 1.5) / uCellTexW;
        vec4 d0 = texture2D(uCellTex, vec2(tx, 0.5));
        vec4 d1 = texture2D(uCellTex, vec2(tx2, 0.5));

        vec3 cp = d0.xyz;     // cell世界坐标
        float cr = d0.w;      // cell场半径
        float p  = clamp(d1.x, 0.0, 1.0);  // cell压力

        float d = length(vWP - cp);        // 像素到cell距离
        float r = d / max(cr, 1e-6);       // 归一化距离

        float w = wd(r);                   // Wendland权重
        ha += hc(p) * w;                   // 累加颜色×权重
        ws += w;                           // 累加权重
    }

    // === Step 2: 加权平均 ===
    vec3 grey = vec3(0.33, 0.36, 0.39);   // 模型底色
    vec3 heat = ws > 0.0 ? ha / ws : grey;

    // === Step 3: 覆盖度混合 ===
    float cf = min(ws * 1.8, 1.0);        // 总覆盖度
    vec3 result = grey + (heat - grey) * cf;

    // === Step 4: Phong光照 ===
    vec3 N = normalize(vWN);
    float NdotL = max(dot(N, normalize(vec3(0.6, -0.8, 0.5))), 0.0);
    float lit = 0.40 + 0.60 * NdotL;      // 40%环境光+60%漫反射
    result *= lit;

    // === Step 5: 透明度 ===
    float a = smoothstep(0.0, 0.002, ws) * 0.01     // 远处微弱的底色
            + smoothstep(0.002, 0.25, ws) * uOp;   // 近处主渐变
    gl_FragColor = vec4(result, a);
}
```

---

## 六、JS端数据流

### 6.1 壳层创建 (ensureHeatmapShell)

```
1. 克隆模型几何体: geo = M.geometry.clone()
2. 计算顶点法线: geo.computeVertexNormals()
3. 将每个顶点沿法线偏移0.6mm:
   pos[i] = pos[i] + normal[i] × mm2u(0.6)
4. 创建Float32Array: texData = new Float32Array(2048 × 8)
5. 创建DataTexture: cellTex = DataTexture(texData, 4096, 1, RGBA, FloatType)
6. 创建ShaderMaterial + Mesh → HM
7. HM添加到RT场景树
```

### 6.2 纹理更新 (syncHeatmapShellColors)

**调用时机**：updateHeatmap() → 每160ms (HEAT_INTERVAL)

**更新逻辑**：
```
1. 从C对象获取所有cell: cells = Object.values(C)
2. 检查缓存key（前32个cell压力+数量）
3. 如果key没变且距上次<300ms → 跳过更新
4. 设置 uCellCount = cells.length
5. 逐cell:
   - 世界坐标转换: applyMatrix4(RT.matrixWorld)
   - 写入Float32Array:
     texData[i*8+0..2] = worldPos.xyz
     texData[i*8+3]    = mm2u(FIELD_R)
     texData[i*8+4]    = cell.pressure
6. cellTex.needsUpdate = true → 触发GPU纹理上传
```

### 6.3 预览开关 (togglePreview)

**进入预览**：
```
1. 保存模型材质属性到 _origMaterial
2. 设置模型: vertexColors=false, color=0x555d65 (纯灰)
3. 调用 ensureHeatmapShell() 创建壳层
4. 设置 HM.visible = true
5. 调用 updateCellPreviewStyle() + HL() 刷新cell圆盘颜色
6. 调用 updateHeatmap() 触发首次渲染
7. 如果模拟已开启: 创建定时器 SIM_INTERVAL(每80ms更新压力) + HEAT_INTERVAL(每160ms更新热力图)
```

**退出预览**：
```
1. 停止所有定时器
2. HM.visible = false
3. 恢复模型材质属性
4. 恢复cell圆盘颜色为默认 0x4ec9b0
```

### 6.4 Cell圆盘颜色映射 (updateCellPreviewStyle)

```javascript
const cp = cellPressures[id] || 0;  // cell压力值 0~1
const r = cp, b = 1 - cp;
ch.material.color = new T.Color(
    r,           // 红色通道 = 压力值
    0.05 * b,    // 绿色通道 = 极低（几乎为0）
    0.70 * b     // 蓝色通道 = (1-压力)×0.7
);
// P=0 → 蓝, P=0.5 → 紫, P=1.0 → 红
```

---

## 七、坐标系转换

```
Cell存储坐标: RT局部空间 (wpToLocal)
        ↓ applyMatrix4(RT.matrixWorld)
Cell shader坐标: 世界空间 (uCP[i])

Shell顶点坐标: Shell局部空间 (position)
        ↓ modelMatrix (= HM.matrixWorld)
Shell shader坐标: 世界空间 (vWP)

距离计算: d = length(vWP - uCP[i])  ← 两者都在世界空间，统一 ✓
```

---

## 八、性能优化措施

| 优化 | 方式 | 效果 |
|------|------|------|
| 缓存key去重 | 前32cell压力+数量组成key | 压力不变时完全跳过纹理上传 |
| 300ms最小间隔 | 纹理上传至少间隔300ms | 避免高频GPU传输导致掉帧 |
| NearestFilter | 纹理采样用最近邻 | GPU采样更快（不需要插值） |
| applyFieldColors空函数 | return 0 | 消除CPU端无效的顶点色计算 |
| cell圆盘renderOrder=5 | 最上层渲染 | 消除与壳层的z-fighting闪烁 |

---

## 九、光照模型

使用简化的单光源Phong模型：
```
lit = ambient + diffuse × NdotL
    = 0.40   + 0.60    × max(dot(N, L), 0)

光源方向 L = normalize(0.6, -0.8, 0.5)  // 右上前方
```

壳层渲染的heatmap颜色乘以光照系数 → 曲面凸起处亮，凹陷处暗 → 3D立体感。

---

## 十、文件清单

```
src/3D编辑器_Shader效果演示.html   — 全部渲染逻辑
├── ensureHeatmapShell()          — 壳层创建(L780-794)
├── clearHeatmapShell()           — 壳层销毁(L795)
├── getHeatmapMesh()              — 返回模型(L796) [CPU方案用]
├── syncHeatmapShellColors()      — 纹理更新(L798-817)
├── applyFieldColors()            — 空函数(L995) [已优化]
├── togglePreview()               — 预览/退出(L1045-1082)
├── toggleSimulate()              — 模拟开关(L1084-1091)
├── updateSimPressures()          — 压力更新(L860-866)
├── simWave()                     — 波浪模拟(L870-874)
├── simUniform()                  — 均匀模拟(L867-869)
├── simPulse()                    — 脉冲模拟(L875+)
├── updateCellPreviewStyle()      — Cell颜色更新(L999-1010)
└── HL()                          — Cell高亮管理(L428-445)

document/update/v1.0.1/beta4/
├── 版本需求.md
├── 问题清单.md
├── 力场渲染方案调研.md
└── 清晰度优化方案调研.md
```
